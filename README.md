# Semantic Search Mini-Project

A lightweight vector search engine demonstrating how to generate embeddings using Hugging Face's `sentence-transformers` and perform nearest-neighbor semantic search using `FAISS`.

## Setup

1. Clone the repository and navigate to the project root.
2. Create a virtual environment (optional but recommended):
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows use `venv\Scripts\activate`
